route:
  receiver: operator-placeholder
  group_by: [alertname, component]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  # Deliberately has no notification integration. Configure and test an approved
  # receiver before relying on alerts; never commit webhook URLs or SMTP passwords.
  - name: operator-placeholder
