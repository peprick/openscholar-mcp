# Testing Strategy

## Unit tests

- Query normalization, mode/provider/local fingerprint separation, and fingerprint stability.
- DOI/arXiv normalization.
- Provider mapping.
- Deduplication/conflict resolution.
- Ranking features and explanations.
- Cache freshness/coverage.
- Search-coordinator default/custom/invalid budgets, deterministic acquisition timeout, supplier non-entry, lock reuse, and interruption-state restoration.
- Search execution default/custom/invalid budgets, successful and failed completion, repeated deadline expiry with no active worker left behind, interrupting worker cancellation, and caller interruption-state restoration.
- Access classification.
- Citation type mapping, identifier normalization, deterministic keys, Unicode, literal authors, and hostile BibTeX escaping.
- Ordered citation batches, strict size/distinct-ID bounds, and all-or-nothing missing-paper behavior.
- Locale-independent collection/tag normalization and literal-safe saved-library queries.
- Embedding contract invariants, exact Ollama runtime/profile/request/digest checks, no-proxy/redirect transport, response-size enforcement, failure-scope translation, and disabled-by-default configuration.
- Backfill command/result invariants, generator selection, bounded verification/generation/stale handling, fail-fast systemic errors, deletion/failure accounting, overflow protection, non-web startup guard, nonzero incomplete-run behavior, and duplicate-generator rejection.
- Multi-provider fan-out, provider-set fingerprints, cursor composition, exact-ID merging, reciprocal-rank fusion, and partial success/failure reporting.
- Search-mode defaults/invariants, opaque local-cursor bounds, deterministic local ranking, and requested-mode/execution-source mapping.
- Durable refresh-job configuration, lease/claim state transitions, retry classification/backoff, stale-completion rejection, worker/scheduler guards, and safe error details.
- OIDC property/JWT validation, route-scope decisions, issuer+subject resolution, protected-resource metadata/challenges, and principal rate-limit identity.
- Frontend OIDC configuration, PKCE/state/nonce callback, ID-token/JWKS/token validation, encrypted session/refresh behavior, exact-Origin enforcement, and job response schemas.
- Privacy-export configuration bounds, fail-fast per-principal/global contention and permit release, count/byte limit arithmetic, exact two-pass byte accounting, unopened targets on admission/preflight rejection, non-closing servlet-stream ownership, and stable `PRIVACY_EXPORT_BUSY`/`PRIVACY_EXPORT_TOO_LARGE` translation.
- Frontend privacy export/deletion validation, native non-buffering download, exact bounded `Content-Length`, fixed no-store/no-transform/nosniff attachment forwarding, identity-encoding request and encoded-upstream rejection, upstream cancellation and mid-stream failure propagation, clearance of the dedicated 140-second timer after response headers, finite abort of a stalled non-success body, hosted-session expiry, accessible confirmation/status behavior, and wording that never claims a download completed or a destructive action was rolled back.
- Frontend backend-JSON media types, declared and streamed byte ceilings, forged/missing `Content-Length`, overflow cancellation, UTF-8/JSON/schema rejection, and safe Problem Details fallback.
- PDF reader source validation, byte/page-count/load/render/canvas/text ceilings, cleanup, generic fallback, and accessible page/zoom/text controls.
- PWA manifest/install fields, production/explicit-browser-test registration, service-worker update headers, auth-proxy exclusions, account-neutral fallback content, cache-only required reader-pair serving, coherent reader-revision enforcement, failed-install cleanup, network-first fixed install assets, the 96-entry runtime-static bound, exact-URL anonymous cache admission, redirect/private/no-store/cookie-vary rejection, safe app-owned cache/version cleanup, blocked IndexedDB upgrade/late-success cleanup, and connectivity probe/state behavior.

## Slice tests

- Spring MVC validation and Problem Details.
- JPA mappings and JSON contracts.
- Raw BibTeX/CSL-JSON response media types, attachment headers, sparse records, and stable citation errors.
- Collection CRUD, saved-paper mutations, owner-scoped not-found behavior, library filters/pagination, and citation-batch attachment contracts.
- Canonical paper details, record-level provenance, immutable credited names, date/year integrity, and stored-access summaries without provider calls.
- Project-owned MCP annotation/specification discovery, closed generated input schemas, safe callback validation,
  and successful structured-output conformance.
- REST/MCP AUTO, ONLINE, and LOCAL contract mapping, including rejection of forced LOCAL refresh and complete result provenance.
- Refresh-job enqueue/list/get/retry contracts and privacy export/confirmed-delete contracts, including exact attachment headers and pre-transaction `429`/pre-commit `422` responses.

## Integration tests

Testcontainers supplies real PostgreSQL/pgvector. Current coverage verifies Flyway from empty, V7-to-V8 library upgrade, V8-to-V9 full-text backfill, constraints/indexes, transactions, idempotent identifier upserts, collection/tag database invariants, literal wildcard handling, deterministic library pagination, owner-scoped access, generated full-text-vector refresh, the GIN index, stopword-only and punctuation-heavy queries, bounded related-paper ranking, venue-only matches, deterministic repeat reads, default-off lexical equivalence, pinned HNSW hybrid ordering, exact lexical-candidate vector coverage, explicit profile/source/coverage fallback modes, operational-error propagation, identical-search miss coalescing, distinct-stripe provider concurrency, explicit concurrent force-refresh behavior, coordination-timeout snapshot rechecks without duplicate provider work, execution-deadline provider interruption with no post-timeout snapshot, successful same-query retry, same-key leader-to-follower handoff, immutable embedding profiles, exact cosine storage, source invalidation, missing-vector cursor paging, and PostgreSQL advisory-lock exclusion.

The current suite also covers the `V13` refresh-job schema, active-target deduplication, `SKIP LOCKED` claims, lease expiry/reclaim and stale-token safety; the `V14` issuer+subject identity constraints; the `V15` search-owner migration/cache indexes; the `V16` mode/origin constraints and origin-aware cache index; current-version-only automatic stale search selection; cross-user search/library rejection; and owner-only privacy export/deletion. Privacy-export persistence tests additionally verify owner-only rows, stable array/tag ordering, cursor mapping, combined-row preflight, the repeatable-read two-pass snapshot, and recovery after an output failure releases admission. Metadata-only local-search coverage must prove zero provider calls in LOCAL, AUTO fallback order, ONLINE exclusion of local results, owner-visible candidate isolation, stable pagination, local continuation after reconnection, and persisted execution provenance. Broader high-contention/load reconciliation remains later work.

The separate owner-scoped LOCAL topic-search development gate is evaluation-only. It seeds synthetic metadata through prior search snapshots and collection membership, runs the production LOCAL path against Testcontainers PostgreSQL, and measures Recall@10, nDCG@10, Precision@1, and MRR. Structural assertions require zero cross-owner leakage and zero discovery-provider calls; no provider network or PDF storage is involved. The gate does not change the production PostgreSQL `english` text-search configuration, ranking formula, API, MCP, or reader UI.

The related-topic reuse development comparison retains a bounded pool of at most 50 production LOCAL results as its control, evaluates at cutoff 10, and uses a separate synthetic `DEVELOPMENT` fixture with new labels. After an author-only development control exposed unrelated feedback, the tuned rule now chooses the first two control-ranked papers carrying `TITLE_EXACT`, `TITLE_PREFIX`, `TITLE_CONTAINS`, or `POSTGRES_FULL_TEXT`; an `AUTHOR_MATCH`-only result cannot seed and takes the exact ranking fallback when no topic-signal seed remains. Each seed yields at most 16 ordered `english` source-title lexemes and at most 25 feedback papers only after target-owner visibility and every original filter are applied. The candidate combines the control at weight `1` with total feedback weight `1`, normalized across the actual non-empty seed lists, using weighted reciprocal-rank fusion with `k = 60`. Equal fused scores prefer control rank, then best feedback rank, with missing ranks last; canonical UUID supplies the stable final tie-break. With no non-empty feedback list, candidate membership and ordering must match the production control exactly and contain only baseline RRF contributions.

The focused reference run measures four queries—three opportunities plus the author control—and keeps a fifth no-seed case as a structural fallback control. It observed baseline-to-candidate macro Recall@10 of `0.542 -> 1.000`, macro nDCG@10 of `0.819 -> 0.968`, five novel relevant papers at 10, and recall gains on `3/3` opportunities, with zero owner leaks, filter violations, rank-one adversaries, provider calls, or candidate-snapshot writes. Explicit adversary exposure at 10 moved from one to three: two owner-visible off-topic controls were introduced at rank two, while the author-substring collision was inherited from the unchanged control. The regression policy caps those observed counts and requires no per-query Recall@10 or nDCG@10 regression. The rule was tuned with development labels and is frozen only for regression; a blind independent holdout and representative performance evidence remain mandatory.

The comparison never calls the globally scoped related-paper endpoint or service, a discovery provider, an embedding generator, a PDF path, or an external scholarly network. Candidate rankings remain evaluation artifacts: no migration, production search path, fingerprint, immutable snapshot contract, UI, REST, MCP, or runtime default changes. The original LOCAL fixture continues to run as a separate regression and isolation gate.

The multilingual lexical development gate is another evaluation-only Testcontainers test. It compares fixed `english`, fixed `simple`, and an enum-allowlisted language-aware profile by running real PostgreSQL 17 weighted vectors, `websearch_to_tsquery`, and `ts_rank_cd(..., 32)` over 15 digest-bound synthetic candidates. This isolates the full-text component rather than reproducing LOCAL title/author/citation boosts or owner visibility. English, German, French, and Spanish contribute to the supported-language gates; the Japanese control is explicitly reported as unsupported, uses `simple` as the language-aware fallback, and is excluded from supported aggregates. After Flyway bootstraps the ephemeral schema, the evaluator executes only scoring and metadata reads, writes no product rows, calls no provider, stores no PDF, and leaves production SQL and the generated `paper.search_vector` on `english`.

The [related-topic reuse scale protocol](RELATED_TOPIC_REUSE_PERFORMANCE_PROTOCOL.md) adds a manual, environment-gated mechanics diagnostic over 100,000 deterministic synthetic papers in the pinned PostgreSQL 17 Testcontainers image. Four fixed workloads run with two warm-ups, 30 measurements, concurrency one, and a warm cache. Structural assertions cover repeated control/feedback/fusion, exact no-seed fallback, nonempty seeded feedback, owner and filter isolation, zero provider calls, and zero candidate-snapshot writes. Nearest-rank p50, p95, and p99 are emitted as record-only JSON to standard output; there is no retained artifact or latency gate. The diagnostic uses no relevance labels, provider network, or PDF path and is not activation or target-deployment evidence.

`RelatedTopicReuseHoldoutPolicyContractTests` pins the preregistered candidate revision and exact candidate/development digests, external three-file boundary and size ceilings, metric formulas and no-relevant/macro/comparison behavior, prospective acceptance gates, absolute author/no-seed controls, first-run/no-retuning/evaluator-freeze declarations, and explicit non-activation interpretation. `RelatedTopicReuseHoldoutBundleTests` uses generated synthetic temporary bundles to exercise the read-only external intake: an internally derived worktree boundary, exact non-symlink layout, bounded JSON that stays strict even with a permissive caller mapper, manifest size/digest binding, internally loaded frozen resources, cross-document identities, complete `0..3` target-visible judgments, lineage/query/filter/adversary requirements, isolated one-dimension filter negatives, candidate-output-field prohibition, and literal development disjointness. It additionally proves that corpus staging does not open malformed judgments, exposes only a path-free and label-free ranking capability, requires a coordinator-issued private completion before judgment loading, binds that completion to the exact manifest and judgment digest/size, rejects replay across same-corpus bundles with different judgments, and exercises every independently reachable ranking-scope path. Judgment release is atomically one-shot for one `VerifiedCorpus` instance and remains consumed after a post-claim failure; a fresh `verifyCorpus` call still creates a new object-local flag, while the separate ledger supplies durable runtime-append-only cross-process finality and `completeRanking` requires its committed one-shot capability before invoking the callback.

`RelatedTopicReuseHoldoutRankingSnapshotTests` freeze exact bundle/corpus/policy/candidate and manifest/judgment commitments, raw IEEE-754 score bits, complete query and ranking partitions, hidden-perturbation invariants, bounds, deep immutability, repeat observations, hidden observations, and structural counters. They also pin the versioned canonical snapshot SHA-256, including sensitivity to key, raw-score-bit, provider-counter, and snapshot-counter changes; the score identity carries that digest. Those classes cover policy, parser, orchestration shape, and evidence shape; their synthetic callbacks alone do not prove ranking execution. `RelatedTopicReuseHoldoutPostgresRankerTests` runs a generated label-free corpus through pinned Testcontainers PostgreSQL using deterministic digest-derived IDs, production LOCAL search, the owner-scoped comparator and frozen RRF, fresh read-only ranking transactions, repeated runs, isolated filters, hidden noninterference, provider/snapshot counters, snapshot DTO round trips, golden UUIDs, full restaging equality, and cleanup assertions. API-shape checks prevent external paths, staged capabilities, and judgments from entering the ranker.

Post-ranking tests require an opaque exact-bundle scoring capability, exercise no-relevant macro exclusions, grade-aware metrics, all 22 ordered gate outcomes, complete failed results, raw-bit repeat and hidden-record comparisons, key-only fallback, immutability, and non-authorization flags. They define an author baseline hit as the sole grade-`3` relevant paper anywhere in the control top ten, while testing Precision@1 and rank-one gates separately. Owner/filter scope counts are unique query/candidate pairs across the union of the control pool, every feedback pool, and the candidate top ten. Source-seal tests bind role, revision, Git mode, safe path, byte length, exact bytes, externally supplied digests, cross-role consistency, and scoped repository-state facts. Clean-checkout collector tests exercise a real standalone clone, collector-only capability issuance, externally bound evaluator/candidate digests, recursive source and Maven configuration inventories, tracked/untracked/ignored changes, skip-worktree launcher tampering, graft rejection, committed candidate drift, malformed Git framing, and HEAD races. Dedicated PostgreSQL ledger tests apply only the isolated migration location, use fixed owner/runtime/auditor roles, force `fsync=on` while overriding a runtime default of `synchronous_commit=off`, and prove exact-once concurrency, finality across changed bundles/evaluators, persistence across a fresh ledger instance and connection, exact constraints/defaults/collations/indexes/ACLs, rejection of session-role masquerading and same-count schema mutations, a verified post-acquisition JDBC network timeout, column-level INSERT-only runtime access, SELECT-only audit access, one-shot corpus/checkout capability binding, mandatory coordinator consumption, fail-closed commit-acknowledgment loss without internal retry, and generated external-layout bundle/clean-Git/ledger/ranking composition.

The package-private PostgreSQL TLS factory adds a closed, non-Spring connection contract. `RelatedTopicReuseHoldoutPostgresTlsConnectionFactoryTests` pins the ordinary typed-input API and bound-ledger return, private reflected source seam, single consumption, and exact pgJDBC 42.7.12 direct-TLS, `verify-full`, LibPQ/strict-SAN-verifier, SCRAM-only, required-channel-binding, no-client-certificate, and timeout settings. It also proves that the preflight does not install JDBC query timeouts, while the ledger integration rejects any `Statement.setQueryTimeout` use; the fixed server startup/transaction timeout avoids pgJDBC's second plaintext CancelRequest socket. Nine strict-verifier tests cover exact case-insensitive DNS SAN acceptance and reject CN-only, wildcard, wrong, non-DNS, malformed, invalid-session, unverified-peer, and parsing-failure inputs. Input negatives cover a query-bearing hostname, wrong CA digest, relative password path, writable/hard-linked/symlinked password, and group-writable/hard-linked/symlinked CA. Proxy-backed tests traverse the expected read-only Phase-A TLS/session, four-database, privilege, role, membership and setting projections; prove rollback plus writable-state restoration on both successful connections; reject an unencrypted session; hide injected secrets and paths; and prove that a changed password consumes Phase B without a second connection open. These proxy results do not independently test every catalog-drift branch, a real certificate or TLS peer, stalled timeouts, CA-owner/ancestor races, effective-UID binding, or a deployed PostgreSQL target. Separate API-shape coverage confines raw `DataSource` ledger construction and direct source access to explicit reflection fixtures.

Operator-workflow tests pin one package-private raw-input execution boundary with no retry, resume, reset, mutation, filesystem publication, or public API. PostgreSQL composition tests exercise clean-checkout failure before claim; one successful in-memory run through claim, ranking, scoring, canonical report creation, and exact verification; replay rejection; committed post-claim failure; and ambiguous commit without ranking or retry. The result is an opaque, non-authorizing `PendingPublication`; the workflow itself writes no evidence files. The scorer returns a privately constructed `VerifiedScoringOutcome` that binds the one-shot `FirstRunEvidence`, identical ranking-snapshot object, and exact scorer result after post-ranking verification. Evidence-report creation and exact verification accept only that outcome, with no package-visible raw-snapshot or raw-result overload; API-shape tests keep its constructors private. Schema v2 binds the durable first-run run key, evaluation protocol ID, policy ID, freeze schema version, source inventory ID, and identical ranking-snapshot object into the canonical source/report bindings and `related-topic-reuse-holdout-report-v2-*` identity. Tests cover equal-but-distinct snapshot rejection, first-run/inventory drift, complete snapshot and scoring projection, raw binary64 hex, content addressing, defensive copies, component-distinct sentinels, one-field binding negatives, and exact-byte replay. Private bundle tests add the closed canonical manifest, exact expected-byte replay, bounded `NOFOLLOW` reads, enforceable POSIX-private modes, single-link files, repository separation, defensive retained metadata, and false authorization flags.

This remains synthetic mechanics evidence only. The package-private, in-memory, filesystem-write-free workflow and TLS factory are not a live isolated command, build launcher, provisioner, or deployed endpoint. There is no real TLS-enabled container/target or stalled-endpoint proof, independent leaf-certificate fingerprint pinning, descriptor-relative pathname safety, `pg_hba.conf`/firewall/storage/administrator/DNS-supervisor enforcement, dedicated-cluster/role provisioning automation, or external-custody implementation. There is also no real independent bundle or label, final evaluator freeze, safe publisher, activation, or custody authorization. Publication remains deferred because portable Java NIO cannot guarantee atomic directory installation without replacing a concurrently created target. The collector, factory, ledger, workflow, and read-only verifier still require separately controlled persistent infrastructure; runtime INSERT-only privileges do not constrain database administrators or storage authority, and the mechanics prove neither trusted time, immutable retention, storage honesty, confidentiality, authorship, process isolation, nor semantic disjointness.

## Provider contract tests

Spring `MockRestServiceServer` fixtures use synthetic or permitted sample responses. Every adapter covers its applicable success, pagination, empty/incomplete results, rate limits, timeouts, malformed payloads, and tolerant schema-evolution cases. OpenAlex additionally verifies exact-limit acceptance and non-retryable rejection of oversized bodies with declared or unknown content lengths, plus duplicate/null/negative/out-of-range/excessive abstract-index positions and oversized reconstructed text while the JSON remains below the transport limit. Europe PMC fixtures verify the default-off/keyless boundary, literal query construction restricted to `SRC:MED` journal articles held in PMC, provider-owned cursor paging, DOI/PMID/PMCID and abstract/metadata mapping, unverified open-access semantics, null discovery `pdfUrl`, and deliberate omission of `fullTextUrlList`. Negative request assertions prove that the adapter uses only REST `/search` and never calls `fullTextXML`, supplementary-file, PDF, or bulk-download routes. DOAJ fixtures verify keyless v4 article-search requests, literal query escaping, unsupported-filter skips, source-reported access semantics, metadata/link-only mapping, public page/window bounds, and rejection of foreign continuation URLs. CORE fixtures verify the fail-closed licence gate, optional backend bearer key, supported v3 search shape, response aliases, and deliberate omission of full text/download URLs. DataCite fixtures verify thesis/dissertation-only query construction, controlled/legacy type handling, provider-owned cursor paging, OA-only short-circuiting, and no discovery OA/PDF claim. Search-configuration tests pin provider concurrency to the default of five and retain its validated bounds. Latch-driven real-transport tests for each discovery adapter stall before headers and after a partial body, asserting stable retryable timeout translation and exactly one upstream request with no automatic retry.

Live-provider tests run manually or on a scheduled, strictly budgeted workflow—not normal pull requests.

The Ollama adapter is exercised through a mock HTTP server with synthetic inputs and vectors. Normal tests do not require an Ollama process, download the 639 MB model, call `/api/pull`, or send scholarly metadata to a hosted provider.

An opt-in Testcontainers evaluation runs the synthetic 18-paper fixture through the real pinned Ollama adapter, performs a complete backfill in an ephemeral PostgreSQL/pgvector database, and measures stable exact cosine neighbors. It is enabled only by `RUN_OLLAMA_VECTOR_EVALUATION=true`; the operator must separately enable the adapter, attest local-only Ollama configuration, and supply the exact full model digest. The gate records Recall@K, nDCG@K, Precision@1, and MRR and is skipped in ordinary CI. It does not enable the application backfill runner, touch a development database, pull a model, or test HNSW.

That manual run also reports an exploratory in-sample hybrid sensitivity sweep over the complete 17-candidate fixture pool. The lexical component is the returned `ts_rank_cd(..., 32)` score, the semantic component is `(cosine + 1) / 2`, and weights `0`, `0.25`, `0.50`, `0.75`, and `1` are all reported. Judgments are applied only after ranking. Endpoint equivalence, repeated component reads, bounded features, deterministic ties, and source/duplicate exclusion are structural gates; no mixed-weight quality floor or product default is selected without independently authored held-out query groups.

A separate opt-in holdout evaluation is enabled only by `RUN_OLLAMA_HOLDOUT_EVALUATION=true`. It proves that the development and holdout paper keys, query keys, source keys, and normalized content fingerprints are disjoint; loads the frozen policy; embeds only the independently authored 26-paper holdout; and evaluates seven query groups over all 25 non-seed candidates. It reads lexical and exact-vector components twice for stability, reports lexical/vector controls and only the frozen `w = 0.50` hybrid, and enforces the predeclared per-query and macro advancement gates. Ordinary CI loads no real model and skips this class.

The first real-model invocation stopped before complete metrics because a valid zero-match Japanese lexical control hit an overstrict structural assertion. Commit `288bf4f` fixed the assertion without consulting labels: empty lexical results remain a measurable control, while vector and frozen-hybrid candidate-pool completeness stays enforced. No policy, fixture paper, judgment, cutoff, or acceptance gate changed. The subsequent run passed all frozen gates: lexical macro `0.857 / 0.648 / 0.286 / 0.571`, exact-vector macro `1.000 / 0.893 / 1.000 / 1.000`, and hybrid macro `1.000 / 0.917 / 0.857 / 0.929` for Recall@K, nDCG@K, Precision@1, and MRR. Five query groups strictly improved nDCG and none regressed.

The separately frozen HNSW gate uses a deterministic 10,000-vector, 1024-dimensional corpus and 20 queries at cutoff 25. Its completed reference run achieved macro Recall@25 `1.0000`, exact p95 `47.491 ms`, approximate p95 `20.082 ms`, and `2.365x` speedup. Ordinary PostgreSQL tests also cover the pinned index/query contract and default-off hybrid orchestration without invoking a real embedding model.

## MCP tests

- Implemented: direct adapter tests for defaults, mode mapping/invariants, bounds, complete provider-provenance mapping, database-only behavior, stable errors, and safe unexpected failures.
- Implemented: safe retryable `SEARCH_COORDINATION_TIMEOUT` and `SEARCH_COORDINATION_INTERRUPTED` tool mappings without nested causes or invented retry-after values.
- Implemented: safe retryable `SEARCH_DEADLINE_EXCEEDED` and `SEARCH_EXECUTION_INTERRUPTED` REST/MCP mappings without nested causes or invented retry-after values.
- Implemented: raw REST and MCP deadline calls prove bounded responses, provider interruption and exit, no post-timeout snapshot, stable versioned tool-error metadata, and no nested-cause leakage.
- Implemented: authenticated raw JSON-RPC initialization, initialized notification, exactly six unique tools, closed mode/source input schemas, output schemas, conservative static annotations, full result provenance, a null-heavy structured search result, and missing/additional/wrong-type argument rejection before provider invocation.
- Implemented: successful raw structured calls for all four database-only tools, using a canonical paper created through the search tool where required.
- Implemented: API-key, duplicate-header, exact-Origin, response-header, and disabled-until-configured security-filter coverage.
- Implemented: hosted missing/invalid/expired/wrong-issuer/wrong-audience/insufficient-scope JWT cases, route-scope enforcement, issuer+subject ownership, protected-resource metadata/challenges, and principal-derived MCP rate-limit keys.
- Implemented: official MCP Inspector CLI discovery and an empty-library `search_saved_library` call against the Compose image.
- Implemented: pinned official conformance `server-initialize` and `tools-list` scenarios with `--spec-version 2025-11-25`; both pass without warnings through the loopback bearer-injection proxy.
- Implemented: locked official JavaScript SDK client smoke over real Streamable HTTP, covering initialization, ping, exact tool/template discovery, the intentionally empty concrete-resource list, and one disposable owner-scoped collection resource read with cleanup.
- Implemented: raw-wire malformed/schema-invalid input, missing-versus-other-owner descriptor equality, oversized request rejection, oversized result rejection without sensitive-content leakage, restricted-access results, and mixed provider success/failure coverage.
- Implemented: the project-owned callback validates successful `structuredContent` before the SDK wrapper and replaces schema drift with the generic versioned `MCP_TOOL_FAILED` result without exposing validator diagnostics.
- Current limitation: the configured 20-second MCP request timeout is not enforced by the stateless MCP Java SDK 2.0 path; the separate 18-second application deadline bounds search use-case execution instead.
- Implemented: repeated JSON-RPC identifiers are shown not to act as idempotency keys; ordinary search fingerprint caching still yields one provider call followed by an exact hit.
- Remaining SDK/transport limitation: client-disconnect and MCP `notifications/cancelled` propagation, framework parsing/serialization and socket-lifetime deadlines, and in-flight JDBC persistence cancellation guarantees.
- Hosted follow-up: live authorization-server/client interoperability, signing-key rotation, token refresh/revocation, and public-edge behavior. Audience/scope/principal ownership is covered synthetically in the application suite.
- Track the canonical frozen `2025-11-25` requirements set as the official runner advances beyond its current fixture-oriented release.

Implemented resource-template coverage includes project-owned unit, integration, and raw JSON-RPC tests that prove:

- initialization advertises a resource capability without `subscribe` or `listChanged`, and `resources/templates/list` returns exactly `openscholar://papers/{paperId}`, `openscholar://collections/{collectionId}`, and `openscholar://searches/{searchId}` with stable JSON metadata;
- `resources/read` returns one bounded `application/json` text resource for each template, with resource-owned deterministic JSON shapes, a 25-item collection page ceiling, and a serialized-size failure path that does not leak discarded content;
- only canonical UUIDs and exact template URIs reach an application use case; malformed or non-canonical IDs, alternate schemes, cross-template paths, extra segments, query strings, fragments, and encoded URI variants fail before dispatch, with raw-wire tests distinguishing fixed matched-template errors from the SDK's standard unmatched-URI error data;
- collection and search handlers map absent and other-owner domain results to indistinguishable safe protocol errors, while hosted OIDC integration coverage proves that the authenticated issuer+subject owner propagates through immediate MCP collection reads;
- paper, collection, and search reads are database-only, verified with zero unintended use-case/provider interactions and with no arbitrary URL dereference, filesystem read, PDF bytes, or source-document fetch;
- `resources/list` returns an empty concrete list, while resource subscriptions and change notifications are neither advertised nor implemented.

The official fixture-oriented conformance suite is not sufficient evidence for this narrower production contract because it expects global resource and subscription behavior that OpenScholar intentionally omits. The production-applicable official subset remains initialization plus tool discovery; project-owned raw-wire tests provide current evidence for resource-template discovery and reads.

If STDIO is added, logs use `stderr`; `stdout` remains protocol-only.

## End-to-end tests

The network-isolated Playwright suite covers search/cache/provider warnings/provenance, immutable continuation, verified versus restricted access, direct PDF.js rendering and keyboard/focus/text controls, individual citation download, paper save, collection creation, reading status/tags, selected citation export, and the personal-data export/confirmed-deletion journey. It blocks unexpected external requests and blocks service workers so request assertions remain visible. This is not a disconnected-browser test.

A dedicated PWA browser scenario permits the production worker, verifies that CacheStorage contains only allowlisted shell assets, confirms successful owned pages are absent, toggles Chromium offline, confirms application unreachability pauses both search actions, loads the account-neutral fallback, runs an accessibility scan, and verifies recovery after reconnection. The encrypted-pack scenario also gates a server collection deletion between request arrival and mutation, begins with an active encrypted pack, proves the real save UI makes no snapshot request while the durable deletion intent is present, rejects a legacy database-version writer, and verifies no active/deletion record after confirmed deletion. A separate delayed-save case proves final-commit rejection. Component and route tests separately cover the advisory `navigator.onLine` signal, the same-origin credentialless `no-store` probe, manual recheck after transient failure, probe-confirmed assistive-technology recovery announcements, continued use of a reachable self-hosted stack with a limited-online-sources warning, fail-closed probe responses, and bypass of hosted session refresh even with an expired cookie. Service-worker policy tests cover network-first install assets, cache-only serving of the required fallback/reader pair, bounded runtime-static cleanup, and deletion of only OpenScholar-owned worker state in non-production runs.

A separate controlled-profile Chromium harness serves synthetic previous, current, incoherent, and uniquely forward-versioned rollback releases from the checked-in worker, shell, and encrypted reader. It proves that an incoherent update becomes redundant and removes its candidate cache while the incumbent remains active; coherent upgrade and schema-compatible rollback workers remain waiting behind an open incumbent client, then activate only after that client closes and a reopened client binds to the new worker. Each activation removes only superseded `openscholar-shell-*` caches, preserves an unrelated cache sentinel and the encrypted IndexedDB envelope/control records byte-for-byte, and still unlocks the saved pack offline with its original passphrase. Public-cache decoys prove that only an anonymous, same-URL allowlisted response is stored: redirects, cookie- or `*`-varying responses, `private`/`no-store` responses, query variants, authorization/range requests, API/auth paths, and document bytes stay out. The rollback release reuses the current schema-compatible implementation under a unique forward revision; this is compatibility evidence, not proof that any historical production artifact can be rolled back safely.

The separate Compose Playwright lane drives the production Next.js build against Spring Boot, PostgreSQL, and a deterministic OpenAlex fixture. It asserts the cold `201/MISS_FETCHED` and repeated `200/EXACT_HIT` boundary, three provider records becoming two canonical DOI-deduplicated results, collection persistence/filtering, BibTeX download, and WCAG scans across search, paper, library, and collection pages. Vitest/React Testing Library retains narrower component/contract coverage. A live IdP login/refresh/logout browser run remains an external interoperability gate because it requires an actual registered client and issuer.

## Evaluation fixtures

Version small metadata-only cases for:

- exact DOI duplicates;
- preprint/published pairs;
- theses without DOIs;
- common-title false positives;
- recent uncited relevant work;
- highly cited irrelevant work;
- multilingual/incomplete records;
- restricted canonical work with an open repository copy.

Expected canonical clusters and top-result ranges are preferred over brittle total ordering.

The versioned `related-metadata-baseline-v1.json` development corpus contains only synthetic metadata and graded `0..3` relevance judgments. Its related-paper evaluations record lexical and exact-vector Recall@K, nDCG@K, Precision@1, and mean-reciprocal-rank floors plus a non-gating hybrid sensitivity sweep; it retains recent uncited work, a DOI-less thesis, Spanish metadata, incomplete metadata, and deliberately difficult cross-domain negatives. The independently authored `related-metadata-holdout-v1.json` adds seven whole query groups across 26 disjoint synthetic papers, including German, English, French, and Japanese metadata, and is paired with the machine-readable frozen `related-hybrid-policy-v1.json`. Exact score decimals and exact total ordering are not ordinary test contracts; the opt-in holdout evaluator records rankings and checks stable repeated reads under the pinned runtime.

The digest-bound `local-catalog-topic-development-v1.json` fixture is separate from related-paper relevance. Its 25 synthetic candidates include 19 target-owner-visible records and six fully judged queries; candidates enter through the same prior-search and collection boundaries as production data, while other-owner and catalog-only records act as isolation decoys. Strict fixture and policy loaders reject unknown, duplicate, trailing, oversized, wrong-typed, dangling, or digest-mismatched input. Its metrics remain test evidence rather than product telemetry, and an independently authored holdout is still required before ranking changes.

The `related-topic-reuse-development-v1.json` fixture and `related-topic-reuse-policy-v1.json` policy form a distinct synthetic `DEVELOPMENT` corpus with newly authored target-query labels and explicit history/visibility cases; they do not replace or silently relabel the LOCAL baseline. The policy binds the 50-paper control pool and cutoff 10, topic-signal-only selection of at most two seeds, 16-lexeme source-title bound, owner/filter-before-ranking rule, 25-result per-seed bound, weighted-RRF `k = 60` formula, normalized feedback weight, control-rank/best-feedback-rank/UUID tie-break, and exact empty-feedback ranking fallback. The frozen seed-selection and ranking code does not read labels, but this rule was chosen after inspecting a development-label author-control failure and is now frozen only as a regression contract. An independently authored blind holdout is still required before any product claim.

The digest-bound `multilingual-lexical-development-v1.json` fixture and `multilingual-lexical-policy-v1.json` policy are separate from both LOCAL owner isolation and related-paper similarity. Their strict loaders reject unknown, duplicate, trailing, oversized, wrong-typed, cross-language, non-allowlisted, or digest-mismatched input. A source-level contract binds the current LOCAL `english` query configuration, scoring weights, tie-break, and V9 generated-vector expression so the comparison cannot silently redefine the production baseline. Its metric floors are engineering evidence only; no production switch is authorized without a disjoint holdout and a reviewed indexing/migration design.

The separate `paper-deduplication-development-v2.json` corpus contains 48 synthetic records, 24 gold clusters, 36 positive pairs, 18 case families, six critical must-separate pairs, and three frozen ingest orders. Strict Java loaders reject unknown fields, invalid values, duplicate or broken references, and policy drift; `paper-deduplication-policy-v2.json` binds the fixture digest and frozen exact-baseline metrics. The Testcontainers evaluator runs every order through the real catalog in rollback-only transactions and checks pairwise/B-cubed/exact-cluster/case-family metrics, safety boundaries, and partition stability. It is evaluation-only: v1 production matching remains unchanged and a separately authored disjoint holdout is mandatory before any metadata fallback can activate.

The PR-gated `EuropePmcProviderQualityEvaluationTests` is a separate deterministic provider-fusion mechanics evaluation. A varied, bounded synthetic corpus passes through the actual `PaperCatalog` and `SearchSnapshotStore`, so the gate covers persistence, exact-signal reconciliation, deterministic fusion, and the metric implementation rather than a parallel in-memory approximation. It checks Recall@20, nDCG@10, Precision@5, MRR within 20, Europe-PMC-unique relevant query coverage, exact-signal deduplication, and metadata completeness. The evaluation makes no live scholarly-provider or other external API call, leaves Europe PMC disabled, and publishes no reader-facing metric; Testcontainers still uses Docker and PostgreSQL networking, and bootstrap may pull dependencies or an image.

The optional live layers are manual and non-gating. Pull requests and clean-clone verification exercise the fused-page capture only against loopback fake application and management servers, without contacting a scholarly provider or retaining a report. Against an isolated `EUROPE_PMC_ENABLED=true` backend, `node scripts/capture-europe-pmc-quality.mjs --base-url http://127.0.0.1:8080` records bounded already-fused pages and private provider telemetry; it cannot recover discarded raw candidates.

`EuropePmcComparativeLiveEvaluationTests` is a separate environment-gated Testcontainers evaluator. Ordinary verification skips it before context creation. When deliberately enabled, it calls only the OpenAlex and Europe PMC metadata adapters once per frozen query, then replays the identical results through isolated and fused rollback-only production persistence. PR-safe tests cover the strict query loader, closed raw-candidate projection, stable keys, pre-truncation trace completeness, artifact byte/permission/digest rules, and fake-provider replay without external networking. Capture payload schema version `2` adds only ranked canonical metadata-presence bits; it serializes no canonical metadata values, and the enclosing manifest stays at schema version `1`.

`EuropePmcComparativeReviewPacketGenerationTests` and `EuropePmcComparativeReviewWorksheetCompilationTests` are separate manual file-only gates between capture and scoring. Ordinary verification skips both. Both runners execute the scorer's full evidence preflight. PR-safe unit coverage proves that packet generation retains the exact blinded order while exposing only an opaque session binding, neutral sequential candidate aliases, safe query text, canonicalized language codes, display-name-only authors, and blinded bibliographic metadata; raw review keys, campaign-bearing bindings, candidate/provider assignments, provider-specific author positions, source-specific corresponding-author flags, provenance, reconciliation, and scenarios stay hidden. The worksheet starts incomplete and contains only its packet digest plus query/alias rows. Compiler coverage verifies the exact immutable packet file, requires every candidate judgment, sorted unique metadata-field names, consistent same-work grouping, canonical alias-based must-separate pairs, an explicit completed-pair review for every query, the exact truthful independence attestation, and an unchanged packet/query/candidate partition. It rejects symlinks, oversized or malformed JSON, unknown/duplicate/trailing fields, incomplete placeholders, reordering, and binding drift before translating aliases to hidden review keys and producing digest-bound v2 judgments. The manual runners use no Spring context, Docker, network, PDF, UI, runtime endpoint, or provider-default switch; their packet, worksheet, and `judgments.json` output is private and ignored. The independent reviewer receives only `review-packet.json` and `review-worksheet.json`. Maven `clean` deletes all output below `backend/target/`, and no real worksheet, label, or judgment packet is checked in.

`EuropePmcComparativeOfflineScoringTests` is a separate manual, environment-gated evaluator over files. Its unit coverage uses synthetic inputs to exercise the exact-five-file evidence verifier, size/digest/schema/review-ready checks, the frozen blinded-review instruction and evidence-scoped order, deterministic review-key derivation, hidden provenance constraints, cross-file reviewer-projection binding, the strict digest-bound independent-judgment loader, exact reviewed-packet verification and judgment binding, the exact frozen query-set resource, and the frozen scoring policy. The scorer assigns at most one highest-grade uncredited gold work per predicted cluster and computes Recall@20, nDCG@10, Precision@5, MRR@20, per-query pairwise deduplication, must-separate violations, expected-field recovery, and Europe-PMC-unique relevant-query coverage. Frozen not-applicable handling keeps no-relevant queries and zero-denominator deduplication rates visible without inventing perfect rates.

Separate score-report bundle coverage requires exactly `manifest.json`, `query-scores.json`, and `score-summary.json` as bounded real, non-symlink files. It exercises missing, extra, symlinked, oversized-payload, oversized-total, malformed, duplicate-key, trailing, digest-mismatched, valid-but-noncanonical-manifest, mixed-result, and wrong-schema/identity cases and requires byte-for-byte equality with the expected v2 scorer output. A fixed identity vector guards the v2 domain and separators; timestamp tests cover canonical capture lineage. Separate path-contract tests reject lexical and resolved replay aliases into `backend/target/`. The manual runner invokes the verifier after publishing new output or, without a write, against an external retained directory before it logs success. Those tests cover score-report integrity; completed-worksheet binding and local promotion have the separate run-seal coverage below. Neither suite proves a stable snapshot on a hostile concurrently mutated filesystem, external retention, longitudinal comparison, reviewer independence, or a default-enablement decision.

Run-seal promotion is a file-only extension of the existing offline scorer rather than a UI, REST/MCP, database, Docker, cloud, or PDF path. Contract tests require `OPENSCHOLAR_PROVIDER_QUALITY_COMPARATIVE_WORKSHEET` and `OPENSCHOLAR_PROVIDER_QUALITY_COMPARATIVE_RUN_SEAL_ROOT` together, accept an existing real external root, and reject roots inside or containing the repository plus direct and resolved symlink aliases. The publisher itself requires an absolute real owner-private root, a private tree, exact source keys, real non-symlink source files outside that root, component and aggregate bounds, and mandatory atomic publication.

`ProviderQualityComparativeRunSealBundleTests` pins the canonical v1 run-seal ID and SHA-256 vector, exact sorted payload paths, byte counts, deterministic layout, source non-mutation, POSIX-private published tree, exact verification, retained no-write verification, and exact-existing-bundle idempotence. It proves that one-byte and whitespace-only completed-worksheet changes produce different run-seal identities even when canonical judgments and score-report identity stay unchanged. It rejects missing, extra, symlinked, and tampered payloads; malformed, duplicate-key, trailing, noncanonical, or wrong-ID seals; untrusted retained declarations or inventory paths; wrong bindings or directory identity; missing or extra source-map entries; source symlinks; aggregate capture overflow; and republishing over a mismatched existing bundle without modifying that bundle.

After publication, the environment-gated runner reopens the promoted bytes and repeats strict capture verification and preflight, review-packet regeneration and exact verification, completed-worksheet compilation, exact canonical-judgment comparison, rescoring to the same result, exact score-report verification, and final run-seal verification before its path-free success record. Ordinary CI now builds fully replayable synthetic empty-result runs against the exact frozen query set and policy and traverses that same environment-independent semantic replay. It does not simulate concurrent ancestor/file replacement, force the filesystem-specific unsupported-atomic-move branch or a failure after staging begins, execute the actual environment gate and process environment, or capture stdout as a log allowlist test; do not treat those branches as independently tested.

The separate environment-gated `EuropePmcComparativeRunSealVerificationTests` entry point accepts one absolute real retained run directory outside the repository. It requires a clean checkout at the exact capture revision, verifies the exact canonical bundle, loads the frozen query set and policy from that revision, and invokes the same full semantic replay used after promotion. `EuropePmcComparativeRunSealVerificationContractTests` accepts the external real-directory contract, rejects repository containment and direct or resolved symlink aliases, and pins the exact privacy-allowlisted success record. The runner itself creates no provider-quality artifact or bundle bytes and does not alter the retained directory; its Maven `clean test` invocation can still create normal ignored build/test output below `backend/target/`. Success exposes only the protocol, verification mode, run-seal ID, run-seal SHA-256, and report ID. The path uses no Spring context, Docker, network, PDF, UI, REST, or MCP surface.

The environment-gated `EuropePmcComparativeLongitudinalComparisonTests` entry point accepts one owner-private, bounded selection file naming between two and sixteen disjoint external run-seal directories. Selection tests reject relative, missing, direct-symlink, repository-contained, resolved-alias, duplicate, ancestor/descendant, malformed, duplicate-key, trailing, oversized, wrong-schema, wrong-protocol, wrong-count, and non-private inputs without modifying them. The runner requires the same clean checkout revision for every seal and performs exact verification, the complete semantic replay, and final exact verification for every selected run before comparison or publication.

`ProviderQualityComparativeLongitudinalComparisonTests` pins a path- and caller-order-independent identity vector, chronological sorting, three-run reversal preservation, adjacent changes, undefined-rate semantics, negative-zero normalization, exact cohort compatibility, distinct run/time identities, full seal-to-score lineage, a closed private report shape, and input non-mutation. `ProviderQualityComparativeLongitudinalReportBundleTests` covers deterministic canonical publication and replay, the exact two-file 8 MiB layout, private permissions, manifest and payload integrity, missing/extra/symlinked/malformed/duplicate/trailing/noncanonical/tampered/mixed output, identity/chronology drift, and no input mutation. Runner contract tests pin the exact opt-in variables, revision boundary, and success-record allowlist. `ProviderQualityComparativeLongitudinalSyntheticWorkflowTests` adds ordinary-CI composition coverage with three time-separated synthetic empty-result runs: reversed selection, exact retained-run semantic replay, chronological comparison, local-only generation, real atomic promotion, repeat retained verification, a valid report rejected against a different replayed cohort, bounded private output shape, path-free success records, byte non-mutation snapshots, and filesystem-supported identity and permission non-mutation snapshots. It starts no Spring context, Docker service, database, provider, or network client. The actual environment gate, clean-Git subprocess, process stdout, and a real retained cohort remain manual because the repository intentionally contains no real retained labels or run seals.

`ProviderQualityComparativeLongitudinalReportPromotionTests` covers the optional file-only handoff into an existing external root: absolute/real/private root validation; repository, selected-run, and source separation through resolved paths; exact two-file bounded copying into private create-new staging; source-byte preservation; mandatory atomic-move failure and bounded cleanup; preservation of unrelated root entries; exact-existing destination idempotence without invoking the mover or rewriting files; and rejection of mismatched existing content without replacement. The injected move seam also models an exact or mismatched destination appearing at publication time, and failure assertions require stable path-free exception chains even when the underlying move exception contains private paths. Generator contract tests pin the optional promotion-root variable plus the exact path-free `generated` and `promoted` success-record allowlist. This focused coverage uses a synthetic comparison and does not execute Spring, Docker, network, provider, UI, REST/MCP, PDF, the environment-gated real-cohort replay, or capture process stdout. It does not emulate a non-POSIX filesystem, simulate concurrent source/ancestor replacement, prove the actual filesystem-specific replace behavior when a target races `ATOMIC_MOVE`, or simulate a failure after publication but before final verification. It also supplies no external authentication, signing, trusted time, confidentiality, immutable retention, deletion policy, access history, or audit controls.

The separate environment-gated `EuropePmcComparativeLongitudinalReportVerificationTests` entry point is a no-provider-quality-write verifier for one externally retained longitudinal report and the existing strict private selection. It requires the clean exact revision, exactly verifies and fully semantically replays every selected run seal, reconstructs the canonical comparison solely from those replayed results, and invokes `verifyExact` rather than publication. The retained report never supplies its own expected cohort or identity, so a valid but unrelated report is rejected.

`EuropePmcComparativeLongitudinalReportVerificationContractTests` pins the exact gate and input names; rejects absent, relative, missing, repository-contained, repository-containing, direct-symlink, resolved-alias, and run-overlapping report paths; proves accepted path validation leaves report bytes unchanged; and pins the privacy-allowlisted success record. Generation and verification invoke one shared replay helper and expose environment-independent coordinators used by the synthetic workflow suite, so ordinary CI traverses the same generation, optional promotion, and retained-verification composition rather than a copied test sequence. Existing selection, run-seal, and report-bundle tests prove their respective retained inputs remain unchanged, and the report tests cover repeatable exact verification, a valid report against a different expected comparison, identity mismatch, and detailed missing/extra/symlinked/malformed/noncanonical/tampered payload cases. Revision and selection-order behavior remain pinned by the comparison runner and comparator suites. The actual environment-variable gate, clean-Git subprocess, process output, real labels, and real retained multi-run cohort remain intentionally manual.

The longitudinal output contains exact operator-only run snapshots plus aggregate and per-query adjacent changes with their denominators and undefined values. It contains no filesystem path, paper metadata, labels, provenance, PDF, or credential and never enters the UI, REST, MCP, database, Spring application, Docker service, or normal logs. Tests prove that code performs no averaging, trend classification, significance calculation, pass/fail threshold, or default-enablement decision. Content-bound capture instants provide reproducible ordering, not trusted time.

This coverage proves deterministic local integrity only under the documented operator-controlled, non-concurrently-mutated filesystem assumption. The ordinary synthetic suite exercises the environment-independent semantic workflow, but it does not execute the actual environment-gated process, clean-Git lookup, or stdout capture. Neither promotion nor standalone run-seal or longitudinal-report verification is authentication, signing, trusted timestamping, WORM/immutable-storage, retention or deletion enforcement, access-history, audit, or confidentiality evidence; those controls remain external. The retained artifacts are operator-only and must never become reviewer artifacts.

The local scoring, verification, and longitudinal workflows use no Spring context, Docker, network, PDF, UI, REST, MCP, or runtime endpoint. Score generation writes a private ignored digest-bound report, report replay writes no new report, run-seal promotion writes the verified bundle to the explicitly configured external root, standalone run-seal verification writes no provider-quality artifact or bundle bytes, longitudinal comparison writes one private ignored deterministic report only after every seal replays and can optionally atomically promote that exact verified report, and retained longitudinal-report verification reconstructs and checks the report without publishing another one. No real labels are checked in, no score is exposed to readers, and no policy threshold or default-enablement decision is implied. A clean live artifact still needs independent relevance and identity labels, a disjoint holdout, and actual time-separated reviewed diagnostics before any default-enablement decision. See [Provider quality evaluation](PROVIDER_QUALITY.md).

## Performance tests

- An opt-in [related-topic reuse scale diagnostic](RELATED_TOPIC_REUSE_PERFORMANCE_PROTOCOL.md) measures control, feedback, and fusion mechanics over 100,000 deterministic synthetic papers in PostgreSQL. Its p50/p95/p99 values are record-only and have no target SLO threshold.
- A checked-in loopback-safe harness measures one forced cold search, repeated exact-cache searches, repeated paper-detail reads, cache-hit ratio, and Prometheus provider error rate. Its 40-sample synthetic Compose reference run passed at 6.944 ms cached-search p95 and 7.305 ms paper-detail p95; see [Local performance evidence](PERFORMANCE_EVIDENCE.md).
- Deterministic provider tests already exercise concurrent fan-out, partial failure, and pre-header/partial-body stalls under configured deadlines. Raw-wire MCP tests enforce inbound-body and structured-result size limits without leaking oversized content.
- Cached search throughput and concurrent-client capacity remain deployment tests.
- Connection-pool saturation, large normalization/persistence batches, and full-text/vector query latency at realistic scale remain target-environment stress tests.
- MCP framework-serialization throughput and socket-lifetime behavior remain transport/deployment tests; application result-size enforcement is already covered.

Use synthetic metadata; commit no copyrighted corpus.

## CI gates

The list below is the target release gate. Unit/integration/frontend checks, controller-inventory-checked OpenAPI, container builds, lockfile-backed MCP conformance, dependency review, CodeQL, Trivy, secret scanning, SBOM generation, immutable-reference/image-policy/VEX validation, deployment/backup guard validation, an offline Playwright workflow, and a deterministic Compose-backed Playwright workflow have checked-in automation. A checked-in workflow is not evidence that a particular revision passed on GitHub; local results are reported separately.

1. Formatting, static, immutable-reference, frozen-tooling, production image-policy, current scoped-VEX, and deployment-configuration checks.
2. Backend unit/slice/integration, OpenAPI inventory, and ArchUnit boundary tests.
3. Frontend unit/contract/security-header tests and production build.
4. Backend, frontend, Caddy, and blackbox-exporter project-owned final-runtime builds and scans; a release additionally rescans their returned registry digests.
5. Network-isolated, true PWA-offline, and Compose-backed Playwright workflows with WCAG 2.2 axe checks.
6. Raw-wire MCP tests plus the production-applicable conformance subset.
7. Dependency, secret, CodeQL, Trivy, and source/runtime SBOM gates.
8. Guarded backup/restore behavior tests and monitoring configuration/rule validation. These fail-closed script tests use mocks; a release still requires a real backup/restore drill.

Conformance and performance suites may run on main/nightly when unsuitable for every pull request.

## Release verification

Run the checked-in aggregate verifier from a clean committed checkout:

```bash
scripts/verify-clean-clone.sh
```

The script fails closed on a dirty source tree so it cannot silently test an older commit. It creates a detached clone of committed `HEAD`, runs host tools with an allowlisted environment and temporary home/configuration, and then composes—not reimplements—the documentation, supply-chain, operations-policy, PostgreSQL/production guard, backend, frontend, standalone PWA/offline Playwright, isolated full-stack Compose Playwright, official MCP SDK, and supported conformance checks. A final Git status check rejects changes to the detached checkout.

Each run generates local credentials and a unique Compose project. Configurable loopback ports, child-specific proxy readiness, failure logs, and trap-backed cleanup constrain removal to that project's disposable containers, network, and PostgreSQL volume. Bootstrap may contact Maven, npm, Playwright, and Docker registries. Runtime search traffic is routed to the checked-in OpenAlex fixture, optional providers are disabled, and the REST/MCP smokes are database-only; this is controlled application behavior, not live-provider evidence.

The script accepts no skip flags. Chromium is installed when absent, but Linux still needs Playwright's operating-system libraries installed by the host or CI image. Port overrides are listed by `scripts/verify-clean-clone.sh --help` for safe parallel use. The source checkout and host configuration are isolated, but this is not a cold or reproducible build: the Docker daemon may reuse pulled images and build layers.

A successful run is bounded local evidence for that committed revision only. Run the verifier only on trusted commits: Maven/Testcontainers receives privileged Docker-socket access, so an untrusted pull request requires a disposable runner. The following remain separate release gates and are not claimed by the script:

- GitHub-native dependency review, CodeQL, Trivy, secret/misconfiguration scanning, and source/runtime SBOM workflows.
- Publication, registry-digest rescanning, signing, attestation, and deployment pinning of all project-owned images.
- A real backup followed by restoration and data verification in an isolated target-like environment; the local guard tests do not perform that drill.
- A real OIDC client/issuer lifecycle, alert delivery/on-call ownership, and target-environment load, accessibility/assistive-technology, penetration, and disaster-recovery exercises.
- Provider, privacy, licence, and jurisdiction-specific legal approval.
